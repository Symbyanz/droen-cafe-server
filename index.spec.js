'use strict';

var expect = require('chai').expect;
var assert = require('chai').assert;
var should = require('chai').should();
var http = require('http');


describe('All tests', () => {
	describe('Test for requestions on server', () => {

		it('Get with headers', (done)=> {
		});

	});
});